Completed the core MVC interceptor hook refactor so `common-aksk` owns AK/SK verification, `common-security` handles only security integration, and AK/SK repository discovery relies on `common-data-jpa`'s shared JPA package registration.

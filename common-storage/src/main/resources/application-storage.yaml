app:
  storage:
    # Optional when exactly one backend is fully configured.
    # type: local
    max-size: 999999
    allowed-extensions:
      - pdf
      - png
      - jpg
      - jpeg
      - gif
      - doc
      - docx
      - ppt
      - pptx
      - xls
      - xlsx
      - txt
      - json
    # local:
    #   path: storage

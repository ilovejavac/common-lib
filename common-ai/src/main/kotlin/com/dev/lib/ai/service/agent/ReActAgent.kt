package com.dev.lib.ai.service.agent

import com.dev.lib.ai.model.ChatSSE

class ReActAgent(

) : Agent {

    override fun run(
        task: String,
        session: ChatSession,
        sse: ChatSSE
    ) {
        // plan

        // generator


        // 反思

        // 更新上下文
    }
}